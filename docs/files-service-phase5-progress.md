# Implementation Progress

- **Template loaded from:** `implement-plan/assets/progress-tracker-template.md`
- **Plan:** `docs/files-service-plan.md` — Phase 5 (Deployment)
- **Status:** `Complete`
- **Updated:** 2026-07-26

`Complete` = all rows `Verified` or user-approved `Descoped` + validation passed + final review `Clear` + nothing material open.

Parent = sole tracker writer under concurrency.

## Tasks / subtasks

Status: `Pending` | `In progress` | `Blocked` | `Verified` | `Descoped`

| ID | Plan ref / requirement | Deps | Status | Acceptance check | Evidence |
|---|---|---|---|---|---|
| T01 | `src/ProxyManager.Files/Containerfile` mirroring `ProxyManager.API/Containerfile`, entrypoint `West94.ProxyManager.Files.dll` | — | Verified | File exists, `podman build` succeeds | Built `west94.com/proxymanager-files:test` locally, image committed successfully; removed after verification |
| T02 | Repo-root `.containerignore` excluding `**/node_modules`, `**/bin`, `**/obj`, `.git` (issue #5) | — | Verified | File exists at repo root | `/Users/jschlach/git/yarp-proxy-manager/.containerignore` created |
| T03 | `systemd/proxymanager-files.container` — image tag, `EnvironmentFile=.env`, `ObjectStorage__*` secrets, `Requires=proxymanager-rustfs.service`+`proxymanager-postgresql.service` | T06 (networking model) | Verified | Unit file present, references correct secrets/network, service can actually address rustfs | `systemd/proxymanager-files.container` created; review found `ObjectStorage__ServiceUrl`/`Bucket` were missing (options would default empty, pointing the AWS SDK at real AWS instead of rustfs) — added `Environment=ObjectStorage__ServiceUrl=http://proxymanager-rustfs:9000` and `Environment=ObjectStorage__Bucket=proxymanager`; also fixed `WantedBy` from `multi-user.target` to `default.target` for rootless `systemctl --user` consistency with sibling units |
| T04 | `systemd/proxymanager-api.container` — missing today (issue #2) | T06 | Verified | Unit file present | `systemd/proxymanager-api.container` created; `ASPNETCORE_HTTP_PORTS=5001` set to match `proxysettings.json` apiCluster address; review found `WantedBy=multi-user.target` inconsistent with sibling units — fixed to `default.target` |
| T05 | Fix `systemd/proxymanager-rustfs.container`: `RUSTFS_ADDRESS=:9000`, `RUSTFS_VOLUMES=/data`, `RUSTFS_CONSOLE_ENABLE=true`; split `ExecStartPre` (no `&&`); fix data-dir ownership | — | Verified (superseded once — see below) | Diff review | Two fixes in sequence. First pass: kept `mkdir -p` + added a second `ExecStartPre=podman unshare chown -R 1000:1000 ...` to replace the broken `&&`-joined single line. **User corrected this**: `podman unshare chown -R 1000:1000` hardcodes a guessed UID (carried over unverified from the original broken unit) and has to be kept in sync by hand. The idiomatic Podman fix is the volume mount's own `U` option, which chowns the host directory to match whatever UID/GID the image actually runs as, automatically, on every start — no guessing, no separate command to maintain. Changed `Volume=%h/proxymanager-rustfs/data:/data` → `Volume=%h/proxymanager-rustfs/data:/data:U,z,rw` (`U` = auto-chown to container UID, `z` = SELinux shared relabel — inert on non-SELinux hosts, correct if ever run on an enforcing one, `rw` = explicit though already default) and dropped the `chown` `ExecStartPre` line entirely; kept `ExecStartPre=mkdir -p ...` since `U` fixes ownership of an existing mount, it doesn't create the host directory |
| T06 | Settle networking model (issue #1) | — | Verified (superseded design — see T12) | All quadlets consistent, no `PublishPort` on pod members | First pass wrongly dropped the pod entirely in favor of standalone `Network=` on every unit — **reverted per user correction, see T12**. |
| T12 | *(correction after user feedback)* Keep `Pod=proxymanager.pod` for single-unit management; put the pod itself on `Network=proxymanager.network` (pod members share the pod's netns/IP, so container-name DNS resolution works exactly as it would standalone — the actual issue #1 bug was that individual pod *members* (`-rabbitmq`, and the never-pod-joined `-rustfs`) carried their own `PublishPort=`, which Podman rejects for pod members) | T05 | Verified | Pod + all backend members consistent; no member carries its own `PublishPort` | `proxymanager.pod` restored: `[Pod] PodName=proxymanager`, `Network=proxymanager.network`, and `PublishPort=` for every port previously host-exposed by an individual member (`8443` proxy, `15672`/`5672` rabbitmq, `9000`/`9001` rustfs — moved from those units' own now-removed `PublishPort=` lines). `proxymanager.container`, `-postgresql`, `-rabbitmq`, `-rustfs`, `-api`, `-files` all use `Pod=proxymanager.pod` (no `Network=` — pod owns it). `proxymanager-ui.container` intentionally stays outside the pod on `Network=proxymanager.network` directly, matching the pre-existing design (the original pod's own `[Unit] Description` only ever named PostgreSQL/Proxy/API as members). Also fixed the pod's own carried-forward `PublishPort=$PORT:8443` undefined-variable bug (same root cause as the one found in the interim standalone `proxymanager.container` — the paired `Environment=PORT=...` lived in `[Container]`/pod-member scope, which only sets container runtime env, never the generating unit's own environment) by hardcoding `PublishPort=8443:8443`; confirmed via grep the app never reads a `PORT` env var. `deploy-vm.sh`'s closing instructions now read `systemctl --user start proxymanager-pod.service proxymanager-ui.service`. Aligned `proxymanager.container`'s `WantedBy` from `multi-user.target` to `default.target` to match every other pod member (touched while redoing this file anyway) |
| T07 | YARP: add `filesRoute` for `/api/files/{**catch-all}` → Files cluster (`PathRemovePrefix`, `BearerToken`, `RequestHeaderRemove: Cookie`, `X-Requested-With` header match) in both proxysettings files | — | Verified | proxysettings updated, route present, valid JSON | Added `filesRoute`/`filesCluster` to both `proxysettings.json` (prod, `http://proxymanager-files:8080/`) and `proxysettings.Development.json` (dev, `https://localhost:5002/`); `python3 -m json.tool` validated both |
| T08 | Add `BearerToken` transform to `apiRoute` in `proxysettings.Development.json` (issue #7) | — | Verified | Diff review | `apiRoute` in dev now has `BearerToken`/`RequestHeaderRemove: Cookie` transforms matching prod |
| T09 | `scripts/build-images.sh`: fix tags to match quadlets, add Files + UI image builds (issue #4) | T01 | Verified (Files/API/Proxy) — UI build blocked by unrelated pre-existing bug | Script runs, tags match quadlet `Image=` values | `bash -n` syntax OK; manually reproduced each `podman build` invocation for proxy/api/files — all succeed. UI build (`npm run build` inside Containerfile) fails on a pre-existing TS error in `NewRouteClient.tsx` (route management, unrelated to Files/Phase 5) — see Decisions |
| T10 | `scripts/deploy-vm.sh`: fix quoted glob (`proxysettings.*.json`) and source directory (`src/ProxyManager/`) (issue #6) | — | Verified | Diff review; glob expands | `bash -n` syntax OK; glob now unquoted and targets `src/ProxyManager/proxysettings.*.json` |
| T11 | *(deviation, not in original plan text)* Fix `dotnet publish`/`restore` in all three .NET Containerfiles to target the specific `.csproj` instead of solution auto-discovery | T01 | Verified | `podman build` succeeds for proxy, API, and files images | Without this, `dotnet publish` with no project arg picks up the whole repo (COPY brings the `.sln`) and collides on duplicate `appsettings*.json` output paths — reproduced on `main`/pre-change API Containerfile too, so this is pre-existing breakage discovered during Phase 5, not introduced by this work. Fixed in `src/ProxyManager/Containerfile`, `src/ProxyManager.API/Containerfile`, `src/ProxyManager.Files/Containerfile` |

## Loop log (optional, keep brief)

| ID | Owner | Worktree / isolation | Checks | Review | Cleanup |
|---|---|---|---|---|---|
| | | | | | |

## Reviews

| Checkpoint | Reviewer | Findings | Disposition | Closure |
|---|---|---|---|---|
| Phase 5 complete (final) | Independent subagent (general-purpose, fresh) | 4: (1) `proxymanager-files.container` missing `ObjectStorage__ServiceUrl`/`Bucket` — CONFIRMED; (2) `PublishPort=$PORT:8443` referenced an undefined systemd-unit-level variable — PLAUSIBLE/CONFIRMED on inspection; (3) `deploy-vm.sh` final instructions told the operator to start the now-deleted `proxymanager.pod` — CONFIRMED; (4) `proxymanager-api.container` `WantedBy=multi-user.target` inconsistent with sibling units' `default.target` (same defect also found, un-flagged by reviewer, in `proxymanager-files.container`) — PLAUSIBLE | Fix now (all 4) | Clear — fixed and re-verified: `ObjectStorage__ServiceUrl`/`Bucket` `Environment=` lines added to `proxymanager-files.container`; `PublishPort=8443:8443` hardcoded (dead `Environment=PORT=...` line removed — grepped confirmed the app never read a `PORT` env var, so nothing else depends on it); `deploy-vm.sh` final message now lists `systemctl --user start` for each service instead of the deleted pod; both `proxymanager-api.container` and `proxymanager-files.container` switched to `WantedBy=default.target` |

## Decisions / deviations

| Item | Need / change | Evidence | Status |
|---|---|---|---|
| Newly-discovered Containerfile bug (not in plan's "Pre-existing breakage" table) | All three .NET `Containerfile`s ran bare `dotnet restore`/`dotnet publish` with no project argument. Since `COPY . ./` brings the whole repo (incl. `.sln`) into the build context, this picks up every project in the solution and `dotnet publish` collides on duplicate `appsettings*.json` outputs — build always failed. Fixed by pointing restore/publish at the specific `.csproj` (T11) | Reproduced failure on unmodified `src/ProxyManager.API/Containerfile` via `git stash` before fixing; confirmed fix by rebuilding all three images successfully | Fixed |
| `proxymanager-files` dev cluster address (`https://localhost:5002/`) | `ProxyManager.Files` has no `Properties/launchSettings.json` yet, so there's no established dev port to reference. Picked `5002` following the `500x` convention used by the API (`5001` target, though API's own `launchSettings` uses `7273`/`5149` — an existing, unrelated inconsistency) | `src/ProxyManager/proxysettings.Development.json` | Flagged — confirm/adjust the port when `ProxyManager.Files` gets a `launchSettings.json` for local dev |
| UI image build fails in `build-images.sh ui` | `npm run build` fails on a pre-existing TypeScript error in `app/(dashboard)/routes/new/NewRouteClient.tsx` (`RouteFormPayload` `destination` mismatch) — unrelated to Files/certificates, route-management code from an earlier phase. Not fixed here: out of Phase 5's deployment scope, and fixing UI type errors isn't part of this plan's phase | Reproduced via `podman build -f src/ProxyManager.UI/Containerfile src/ProxyManager.UI` | Not fixed — flagged for the user / a future UI fix, separate from Phase 5 |
| `west94.com/proxymanager-api` image tag | Plan issue #4 only says tags "match no quadlet" without prescribing the correct name for the (previously nonexistent) API quadlet. Named it `proxymanager-api` to match the `proxymanager`/`proxymanager-ui`/`proxymanager-files` naming convention already used elsewhere, rather than reusing the old `proxy-api` tag | `systemd/proxymanager-api.container`, `scripts/build-images.sh` | Applied |
| Dropped the pod entirely instead of fixing the actual `PublishPort`-on-pod-member conflict | User corrected this after Phase 5 was first marked complete: the pod was added deliberately for whole-app management ergonomics (`podman pod start`/`stop` etc.), and a pod placed on `Network=proxymanager.network` still gets container-name DNS resolution for every member, since pod members share the pod's network namespace/IP and are distinguished by port, not IP. Podman's actual restriction is narrower — individual pod *members* cannot carry their own `PublishPort=`, only the pod can. Reverted to keeping the pod (T12) and moved the previously-member-level `PublishPort=`s up to `[Pod]` | `systemd/proxymanager.pod` restored; see T12 | Reverted/fixed |
